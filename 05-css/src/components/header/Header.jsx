import styles from './Header.module.css'

const Header = () => {
  return (
    <div>
      <h2 className={styles.header}>Hello</h2>
      <button>
        view
        </button>
    </div>
  )
}

export default Header